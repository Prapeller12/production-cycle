# Результаты проверки дополнений этапов — 10.09.2026

## Проверяемые изменения

- предзаполнение формы редактирования сохранёнными данными этапа;
- наследование исполнителя, адресата, начала и дедлайна при создании дочернего этапа;
- сохранение необязательного комментария в JSON и SQLite;
- совместимость существующей SQLite-базы и JSON без поля `comment`;
- адресат этапа в реестре при сохранении инициатора у основного проекта;
- знак `!` при равенстве дедлайна этапа дедлайну основного проекта;
- красная обводка строки при превышении дедлайна основного проекта;
- неизменность TXT-контракта: 9 колонок исходящего файла, 8 колонок входящего, комментарий не экспортируется.

## Локальная проверка

Команда: `node --test tests/domain.test.cjs`

Результат: 27 из 27 тестов пройдены, ошибок и пропусков нет.

Дополнительно:

- `node --check frontend/ui/app.js` — успешно;
- `git diff --check` — успешно;
- локальная Rust-проверка не выполнена: `cargo` отсутствует в рабочей среде;
- браузерная и Windows portable-проверки выполнены штатным GitHub Actions после публикации коммита.

## GitHub Actions

Итоговый коммит реализации: `2cee167585c35b4e83f02ed6aa4357f23cd103dc`.

Итоговый запуск: [Production cycle prototype checks №21](https://github.com/Prapeller12/production-cycle/actions/runs/34474232666) — успешно.

- `contracts`: 27 из 27 модульных тестов и полный браузерный сценарий — успешно;
- предзаполнение формы, комментарий, отображение адресата и оба предупреждения дедлайна проверены браузерным сценарием;
- визуальный снимок интерфейса сформирован и просмотрен;
- `windows-host`: 9 из 9 Rust-тестов — успешно;
- миграция существующей SQLite-базы с добавлением `comment` — успешно;
- сборка и запуск portable-архива из пути с кириллицей и пробелами — успешно;
- автономный запуск со встроенным WebView2, без Python и Node.js в `PATH`, с блокировкой исходящего трафика — успешно.

Промежуточные запуски №19 и №20 выявили две ошибки исключительно в новых браузерных ожиданиях: неоднозначный селектор кнопки закрытия и диапазон диаграммы Ганта без учёта контрольного этапа за дедлайном проекта. Ожидания исправлены; рабочая логика приложения в этих точках не изменялась.
