# Результаты проверки редактора справочников — 10.09.2026

## Проверяемые изменения

- формирование справочников из фактически сохранённых значений SQLite;
- категории: предприятия/адресаты, исполнители/получатели, инициаторы/подписанты, названия проектов и названия этапов;
- точная замена выбранного написания во всех сохранённых проектах категории;
- объединение дублей, различающихся регистром, без автоматического изменения данных;
- подсчёт числа использований и затронутых проектов;
- автоподстановка сохранённых вариантов во все соответствующие поля;
- синхронизация открытого проекта после замены;
- запрет пустых значений, TAB и переносов строк;
- неизменность TXT-контракта 9/8 колонок, PDF, структуры и диаграммы Ганта.

## Локальная проверка

Команда: `node --test tests/domain.test.cjs`

Результат: 28 из 28 тестов пройдены, ошибок и пропусков нет.

Дополнительно:

- `node --check frontend/ui/app.js` — успешно;
- `node --check frontend/services/backend.js` — успешно;
- `node --check tests/browser.cjs` — успешно;
- `git diff --check` — успешно;
- локальная Rust-проверка недоступна: `cargo` отсутствует в рабочей среде;
- браузерная, Rust и Windows portable-проверки будут дополнены после публикации коммита и завершения GitHub Actions.

## GitHub Actions

Статус: ожидает публикации коммита.
